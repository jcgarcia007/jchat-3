export default function ReservationsPage() {
  return (
    <div>
      <h1
        style={{
          fontSize: "22px",
          fontWeight: 700,
          color: "var(--db-text-primary)",
          marginBottom: "8px",
        }}
      >
        Reservations
      </h1>
      <p style={{ fontSize: "14px", color: "var(--db-text-secondary)" }}>
        Table bookings, waitlist, and reservation calendar — coming in Task 3.11.
      </p>
    </div>
  );
}
