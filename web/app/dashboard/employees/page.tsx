export default function EmployeesPage() {
  return (
    <div>
      <h1
        style={{
          fontSize: "22px",
          fontWeight: 700,
          color: "var(--db-text-primary)",
          marginBottom: "8px",
        }}
      >
        Employees
      </h1>
      <p style={{ fontSize: "14px", color: "var(--db-text-secondary)" }}>
        Add staff accounts, assign roles, and manage access permissions — coming in Task 2.9.
      </p>
    </div>
  );
}
